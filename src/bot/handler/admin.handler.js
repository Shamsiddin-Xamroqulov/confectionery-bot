import bot from "../bot.js";
import { languageKeyboard } from "../keys/keyboard.js";

export const adminHandler = async (msg, username) => {
    const chatId = msg.chat.id;

    // bot.sendMessage(chatId, )
};